module.exports = {
    siteUrl: `https://arh.gtsb.io`, // Site domain. Do not include a trailing slash!

    postsPerPage: 6, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `AkhyarRH`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `I write some stuff`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `AkhyarRH`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#2d2d2d`, // Used for Offline Manifest
    themeColor: `#5cb464`, // Used for Offline Manifest
}
